.DS_Store
.claude/settings.local.json

# Private — never commit the business plan or anything like it
*.docx
*.pages
*.numbers

# Private — CV and other files Claude generates for me
Claude outputs/

# Personal and large files — never publish these to the live site
Business address invoice.pdf
*.zip
export/
